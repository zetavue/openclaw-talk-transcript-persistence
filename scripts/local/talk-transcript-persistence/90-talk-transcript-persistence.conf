[Service]
# Keep local WebUI/voice Talk transcript persistence across npm package updates.
# This does not pin OpenClaw to an old build; it patches the installed dist bundle if needed.
ExecStartPre=%h/.openclaw/tools/ensure_talk_transcript_persistence_patch.py
